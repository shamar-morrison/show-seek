1. you did not update the forestore permissions, i am getting an error for persisting personal onboarding completition.

2. did you write the backfill script

3. Add on boarding step after the region selection that asks the user to select their streaming providers (this will be based on their selected region if it can be) they use, fetch that data from the tmdb providers endpoint (we have one in the code already I believe) and present them in a 3 column grid layout with the providers image. this data will not be stored or persisted anymore it is simply for aesthetic (do not tell the user this though)
