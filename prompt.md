1. it should be an option inside the truesheet alongside the other options
2. i think the genre data is fetched on app launch and stored in async storage or react query but i'm not sure, scan the codebase and see, if thats not the case, you can check for exising hooks or build it from the items in the list
3. the filters should not persist
