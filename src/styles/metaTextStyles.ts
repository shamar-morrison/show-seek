import { StyleSheet } from 'react-native';
import { COLORS, FONT_SIZE } from '@/src/constants/theme';

export const metaTextStyles = StyleSheet.create({
  secondary: {
    fontSize: FONT_SIZE.s,
    color: COLORS.textSecondary,
  },
});
